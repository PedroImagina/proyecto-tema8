import { Router } from 'express';
// import { ... } from '../controllers/userController';

const router = Router();

// Rutas de usuarios
// router.get('/', ...);
// router.get('/:id', ...);
// router.post('/', ...);
// router.put('/:id', ...);
// router.delete('/:id', ...);

export default router;

