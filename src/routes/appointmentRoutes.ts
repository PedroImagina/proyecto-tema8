import { Router } from 'express';
// import { ... } from '../controllers/appointmentController';

const router = Router();

// Rutas de citas
// router.get('/', ...);
// router.get('/:id', ...);
// router.post('/', ...);
// router.put('/:id', ...);
// router.delete('/:id', ...);

export default router;

