import { Router } from 'express';
// import { ... } from '../controllers/serviceController';

const router = Router();

// Rutas de servicios
// router.get('/', ...);
// router.get('/:id', ...);
// router.post('/', ...);
// router.put('/:id', ...);
// router.delete('/:id', ...);

export default router;

